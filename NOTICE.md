# Attribution and licensing

This project incorporates behavior migrated from `autoMaxPPHPL`, authored by
Rabbit House and published with the namespace
`https://github.com/gangbaRuby`. The supplied source userscript declares
`AGPL-3.0` in its metadata header.

The combined SimComps Little Tools distribution is licensed under
`AGPL-3.0-or-later`. The userscript header retains this attribution and the
minimum userscript permission needed by the migrated VWAP lookup:
`GM_xmlhttpRequest` with `@connect api.simcotools.com`.

The legacy AutoMaxPPHPL update host and privileged `unsafeWindow`/
`GM_registerMenuCommand` integrations are not part of this distribution.
